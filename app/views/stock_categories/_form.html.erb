<%= form_with(model: stock_category, class: "form") do |f| %>
  <% if stock_category.errors.any? %>
    <div class="error-messages">
      <h2><%= pluralize(stock_category.errors.count, "error") %> prohibited this category from being saved:</h2>
      <ul>
        <% stock_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_area :description, class: "form-control" %>
  </div>

  <div class="form-actions">
    <%= f.submit "Save", class: "button primary" %>
    <%= link_to "Cancel", stock_categories_path, class: "button" %>
  </div>
<% end %> 